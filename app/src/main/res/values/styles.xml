<resources>

    <!-- Base application theme. -->


    <style name="AppTheme.OverlapSystemBar" parent="AppTheme">
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
<!--        <item name="colorPrimary">@color/colorPrimary</item>-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="ChipsStyle">
        <item name="android:checkable">false</item>
        <item name="android:clickable">false</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Light" />

<!--    theme for text input layout-->
    <style name="ThemeTextInputLayout">
        <item name="colorPrimary">#1735DD</item>
    </style>

    <style name="DividerDetailsStyle">
        <item name="android:layout_margin">@dimen/general_margin_16dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

</resources>
